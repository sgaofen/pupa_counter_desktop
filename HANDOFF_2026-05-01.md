# Agent Handoff — Pupa Counter Desktop (2026-05-01)

Stephen is moving this work from his Windows demo machine over to his
**Mac**, where the sister repo `pupa_counter_publish` (training scripts
+ labeling GUI) is believed to live. This doc is the single source of
truth for picking up the Mac session cold.

---

## Where things stand on the Windows side

### Repos checked out locally

- `C:\Users\YY\Documents\pupa_counter_desktop` — Electron app, branch
  `main` at `64015b7` ("Rewrite README to match the current state",
  2026-04-28). **Remote is ahead** (`52af66...` is current `origin/HEAD`).
  Stephen explicitly chose to run the older local copy for today's
  demo rather than pull the newer remote.
- `C:\Users\YY\Documents\pupa_counter_v6` — sister inference repo,
  branch `main` at `dfec175` ("Auto-detect best inference backend; add
  multi-machine venv bootstrap"). Up to date.
- `C:\Users\YY\Documents\pupa_counter_v6\.venv` — bootstrapped via
  `scripts/setup_venv.py`, runs on Intel XPU (Arc 140T iGPU, 16 GB).
  CNN daemon launches in ~1.2 s, model = `pupa_counter_v12.pt`,
  classifier = `peak_filter_clf.pkl`.

### What was changed today (uncommitted)

`pupa_counter_desktop/` working tree:

1. `src/adapters/scannerAdapter.ts` — saved deviceId is now validated
   against the live WIA enumeration on every scan. If the saved ID is
   stale (re-plug, driver swap), we drop it, auto-pick the first live
   device, and write the new ID back to localStorage. Fixes the
   `RuntimeException: device not found: SWD\EsclUsb\{...}` Stephen
   hit when today's session resurrected a stale eSCL ID from a prior
   run while WIA had re-enumerated the LiDE 300 under its legacy GUID.

2. `src/store/sessionStore.ts` — `isoNow()` now returns LA wall-clock
   (`America/Los_Angeles`) instead of UTC. Format unchanged
   (`YYYY-MM-DD HH:MM:SS`, via `sv-SE` locale).

3. `src/pages/ScanView.tsx` — the read-only "Date & time" field in the
   Image Information panel uses the same LA wall-clock formatting.

These are HMR-applied; nothing else was touched. No commit was made.

### What was NOT touched

- `electron/main.js` filename stamps still use UTC ISO
  (`scan_<UTC>.png`). Renaming would break the existing scan log;
  decide on Mac whether to convert.
- `src/pages/DatabaseView.tsx:101` export filename prefix still uses
  UTC ISO date for the same reason.

---

## Today's demo: what actually happened

1. Started the desktop app via `npm run dev` from
   `pupa_counter_desktop/`. Vite + Electron both up, CNN worker
   pre-warmed on Intel XPU.
2. First scan attempt failed with stale-device-ID error (cause:
   localStorage held an `SWD\EsclUsb\{35a7deeb-...}` from a previous
   eSCL enumeration; current WIA only sees the LiDE 300 under
   `{6BDD1FC6-810F-11D0-BEC7-08002BE2092F}\0000`).
3. Patched `scannerAdapter.ts` to self-heal stale IDs (above).
4. Stephen scanned 8 sheets between 13:04 and 13:14 PT. **First 2
   are paper-only test sheets, last 6 have real pupae.**
5. All 8 PNGs saved to:
   `C:\Users\YY\AppData\Roaming\pupa-counter-desktop\scans\`
6. Copied verbatim to:
   `C:\Users\YY\Documents\pupa_counter_v6\data\2026-05-01_lide300_300dpi\`
   (with a README.md describing scan params + the exclusion).

---

## The training problem (this is why we're handing off to Mac)

Stephen's working assumption is that the existing `pupa_counter_v12.pt`
won't perform on the LiDE 300 output because of the **dpi mismatch**:

| Source | DPI | File size | Notes |
|---|---|---|---|
| v12 training set (1200 dpi scanner) | 1200 | ~25 MB | what the model was trained on |
| LiDE 300 today | **300** | ~6.3 MB | this scanner caps at 600 |

He wants to either fine-tune v12 or train from scratch on the new
scanner's output. Today's batch (6 real pupae sheets) is **way too
few** for from-scratch — v6 used 60, v12 used 99. Realistic plan:
fine-tune with ~30 sheets, retrain fresh with ~80+.

But: **the tooling isn't on this machine.**
- `pupa_counter_v6` is an inference-only repo. No `train.py`, no
  labeling GUI.
- v6's README (line 320–323) points to a sister repo
  `pupa_counter_publish/` with:
    - `scripts/label_whole_scan.py` — point-click labeling GUI
    - `scripts/train_v12.py` — U-Net training
    - `scripts/train_clf_v5_and_eval.py` — classifier training
    - `tmp/whole_scan_labels.json` — current label set
- That repo is **not on this Windows machine** and is **not visible on
  GitHub** (`sgaofen` has no `pupa_counter_publish` repo). The
  `HANDOFF_2026-04-16.md` (in `pupa_counter_v6/`) puts the original
  training session on macOS at `/Users/stephenyu/Documents/pupa_counter_v6`
  — so the publish repo most likely lives on Stephen's Mac.

---

## Action items for the Mac session

In order of priority:

1. **Locate `pupa_counter_publish/` on the Mac.** Most likely path:
   `~/Documents/pupa_counter_publish/`. If it's not there, try
   `~/code/`, `~/repos/`, or check for an old git remote. If it's
   genuinely gone, recover from time-machine / iCloud / a private GitHub
   repo before doing anything else.

2. **Ground-truth check before committing to retrain.** Run
   `python pupa_counter.py data/2026-05-01_lide300_300dpi/scan_2026-05-01T20-06-44-194Z.png`
   on the Mac with the existing v12 weights. If v12 is already passable
   on 300 dpi LiDE output, the right move is fine-tune, not retrain.
   Stephen's "this scanner can't use the old model" assumption is
   plausible but unverified.

3. **Pull the 8 scans onto the Mac.** Source of truth on Windows:
   `C:\Users\YY\Documents\pupa_counter_v6\data\2026-05-01_lide300_300dpi\`
   (incl. README.md with the exclusion note for files 1–2).

4. **Commit the desktop-app fixes.** Three files modified today
   (scannerAdapter.ts, sessionStore.ts, ScanView.tsx). The
   stale-deviceId fix is genuinely useful and should ship; the LA
   timezone change is per Stephen's request. Commit message draft:

   ```
   Self-heal stale scanner deviceId; show LA time in image info

   - scannerAdapter validates saved deviceId against live WIA list and
     re-picks if missing (fixes SWD\EsclUsb\... not-found error after
     re-plug / driver swap)
   - isoNow() and ScanView's Date & time field use America/Los_Angeles
     wall-clock instead of UTC
   ```

5. **Decide: fine-tune vs from-scratch.** Stephen said "从头就行了"
   today, but with only 6 usable scans that produces a useless model.
   Either scan ~70 more sheets first, or fine-tune.

---

## Inventory of artifacts produced today

| Artifact | Path |
|---|---|
| 8 raw scans (originals) | `C:\Users\YY\AppData\Roaming\pupa-counter-desktop\scans\scan_2026-05-01T20-*.png` |
| Same scans, packaged for training | `C:\Users\YY\Documents\pupa_counter_v6\data\2026-05-01_lide300_300dpi\` |
| Training-data README (describes dpi, exclusions) | `C:\Users\YY\Documents\pupa_counter_v6\data\2026-05-01_lide300_300dpi\README.md` |
| This handoff | `C:\Users\YY\Documents\pupa_counter_desktop\HANDOFF_2026-05-01.md` |
| Modified desktop-app source (uncommitted) | `src/adapters/scannerAdapter.ts`, `src/store/sessionStore.ts`, `src/pages/ScanView.tsx` |

---

## Open questions Stephen still needs to answer

- DPI ceiling: did he confirm the LiDE 300 is locked at 300 dpi for the
  desktop app, or could it go to 600? (driver supports up to 1200 dpi
  interpolated, 600 native — worth retesting at 600 if image quality
  matters more than file size).
- Same scan protocol as before (orientation, lighting, paper type)?
  The v12 model has known weaknesses on left-edge truncation and
  handwritten notes; if the new protocol differs, those failure modes
  may not transfer.
- Does Sarah / Anthony need a working pipeline by a specific date?
  That sets the fine-tune-vs-retrain bar.
